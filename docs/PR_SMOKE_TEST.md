# Pull request smoke test

This documentation-only file verifies that the repository's automated pull
request workflow can create a branch, commit a change, and open a pull request.
It does not alter the Offline Reader application or its runtime behavior.
